# Cortex / Unknown — nix devShell env
# 用 direnv 自带 `flake` 替代 `use flake`，profile 文件复用率高、reload 触发条件更可控
# watch_file 显式声明 direnv 监听列表；不写就意味着 direnv 在 cd 时只看自己的 hash
watch_file flake.nix
watch_file flake.lock
watch_file .env
# 触发时如果 nix profile 已经存在（上次求值过），direnv 会增量更新而不会全量重写
use flake

# 本地密钥：OPENAI_API_KEY（.env 已被 gitignore，不会提交）
dotenv .env
